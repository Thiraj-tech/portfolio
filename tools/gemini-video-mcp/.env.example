# Copy this file to .env and fill in real values. .env is gitignored — never commit it.

# Google AI Studio API key (https://aistudio.google.com/apikey) with billing enabled for Veo video generation.
GEMINI_API_KEY=
